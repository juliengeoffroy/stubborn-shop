{% extends 'base.html.twig' %}

{% block title %}Détails de la commande{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>🧾 Commande #{{ order.id }}</h1>

    <p><strong>Date :</strong> {{ order.createdAt|date('d/m/Y H:i') }}</p>
    <p><strong>Client :</strong> {{ order.user.email }}</p>
    <p><strong>Total :</strong> {{ order.total }} €</p>
    <p><strong>Statut :</strong> {{ order.status }}</p>

    <h4 class="mt-4">Articles</h4>
    <ul>
        {% for item in order.orderItems %}
            <li>{{ item.quantity }} × {{ item.sweatshirt.name }} ({{ item.price }} €)</li>
        {% endfor %}
    </ul>

    <a href="{{ path('admin_orders') }}" class="btn btn-link mt-3">⬅️ Retour</a>
</div>
{% endblock %}
