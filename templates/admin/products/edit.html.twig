{% extends 'base.html.twig' %}

{% block title %}Modifier le Sweatshirt{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">✏️ Modifier : {{ sweatshirt.name }}</h1>

    {{ form_start(form) }}

    {% include 'admin/products/_form_fields.html.twig' %}

    <button class="btn btn-primary mt-3">Mettre à jour</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_products') }}" class="btn btn-link mt-3">⬅️ Retour</a>
</div>
{% endblock %}
