{% extends 'base.html.twig' %}

{% block title %}Boutique{% endblock %}

{% block body %}
<h1 style="text-align: center; margin: 30px 0;">Notre boutique</h1>

<div style="max-width: 1200px; margin: auto; display: flex; flex-wrap: wrap; gap: 30px; justify-content: center;">
    {% for sweat in sweats %}
        <div style="border: 1px solid #ccc; padding: 15px; width: 200px; text-align: center; border-radius: 10px;">
            <img src="{{ asset('images/' ~ sweat.image) }}" alt="{{ sweat.name }}" style="width: 100%; height: 200px; object-fit: contain;">
            <p><strong>Nom :</strong> {{ sweat.name }}</p>
            <p><strong>Prix :</strong> {{ sweat.price }} €</p>
            <a href="#" class="btn btn-outline-primary">VOIR</a>
        </div>
    {% endfor %}
</div>
{% endblock %}
